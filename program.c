#include <stdio.h>
int main ()
{
  /* variable definition: */
  float x, y, z;

  /* variable initialization */
  x = 10.0;
  y = 0.0;
  z = x / y;
  printf("Integers (x,y) and quotient (z) are : %f,%f,%f \n", x,y,z);
   return 0;
}
